<h1>Let's add a restaurant !</h1>

<%= simple_form_for(@restaurant) do |t| %>
<%= t.input :name %>
<%= t.input :address %>
<%= t.input :phone_number %>
<%= t.input :category, collection: Restaurant::CATEGORY %>
<%= t.submit %>
<% end %>

<%= link_to "Back to home page", restaurants_path %>
